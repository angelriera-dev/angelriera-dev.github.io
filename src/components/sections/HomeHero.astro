---
import bg from "@/assets/media/bg/hero-haker-hoodie_100.png";
import Btn from "@/components/ui/Btn.astro";
import { siteCopy } from "@/content/i18n/site";
import { getLocale } from "@/utils/i18n";

const locale = getLocale(Astro.url.pathname);
const copy = siteCopy[locale];
const basePath = locale === 'en' ? '/' : `/${locale}/`;
---

<style define:vars={{ image: `url("${bg.src}")` }}>
    .home_hero {
        background:
            linear-gradient(rgb(0 0 0 / 64%), rgb(0 0 0 / 0%)), var(--image);
        background-position: 50% 50%;
        background-origin: content-box;
        background-repeat: no-repeat;
        box-shadow: inset 0px -2rem 15rem 2rem rgb(0 0 0);
        background-size: 150%;
        background-attachment: fixed;
    }
    @media (min-width: 768px) {
        .home_hero {
            box-shadow: inset 0px 1rem 9rem 11rem rgb(0 0 0);
            background-size: 80%;
        }
    }

    .heroTitle {
        color: #cccccc;
        margin-bottom: 4px;
        text-shadow:
            1px 1px 1px #000000,
            -2px 2px 6px #838283,
            2px -2px 6px #000000;

        line-height: var(--sl-line-height-headings);

        & > span.text-secondary {
            text-shadow:
                1px 1px 1px #000000,
                -2px 2px 6px #003750,
                2px -2px 6px #003750;
            line-height: var(--sl-line-height-headings);
        }
        & > span.text-primary {
            text-shadow:
                1px 1px 1px #000000,
                -2px 2px 6px #33368d,
                2px -2px 6px #33368d;
            line-height: var(--sl-line-height-headings);
        }
    }
</style>

<article
    id="home"
    class="home_hero relative flex overflow-auto bg-fixed text-center text-white
    flex-col items-center justify-center"
>
    <div class="content-shell my-auto">
        <div
            class="grid h-min grid-cols-1 justify-items-center gap-10 pb-[9rem] md:pb-0"
        >
            <h1 class="heroTitle text-4xl font-bold uppercase md:text-6xl">
                <span class="text-primary">{copy.hero.primary}</span>
                &
                <span class="text-secondary">{copy.hero.secondary}</span>
                <br class="hidden md:inline-block" />
                {copy.hero.titleSuffix}
            </h1>

            <h2 class="max-w-4xl text-lg md:text-3xl">
                {copy.hero.subtitleLine1}
                <br />
                {copy.hero.subtitleLine2}
            </h2>

            <div class="flex flex-wrap items-center justify-center gap-4">
                <a href={`${basePath}portfolio`}>
                    <Btn theme="primary" size="lg"> {copy.hero.portfolioCta} </Btn>
                </a>

                <a href="#contact">
                    <Btn theme="secondary" size="lg"> {copy.hero.contactCta} </Btn>
                </a>
            </div>
        </div>
    </div>
</article>
